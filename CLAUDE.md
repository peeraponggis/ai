# กติกาของโปรเจกต์นี้

## ห้ามสร้างสิ่งที่ทำให้ผู้ใช้เชื่อว่ามีอะไรเกิดขึ้น ทั้งที่ไม่มี

กติกานี้ตั้งขึ้นเพราะเคยละเมิดมาแล้วจริงในโปรเจกต์นี้ ไม่ใช่ข้อควรระวังตามทฤษฎี

**สิ่งที่เกิดขึ้น:** แอปคำนวณโซล่าร์ (ก่อน v1.0.8) หน่วงเวลา `await delay()` รวม
5.3 วินาที โดยไม่ได้ทำอะไรในช่วงนั้น แล้วพิมพ์ชื่อโมเดล `qwen2.5:32b`
`deepseek-r1:32b` `llama3.1:70b` `claude-3-5-sonnet` `gpt-4o` `mistral-large`
ลงหน้าจอราวกับกำลังเรียกใช้งาน ทั้งที่แอปไม่มีสิทธิ์ `INTERNET` ด้วยซ้ำ
จึงเรียกอะไรไม่ได้เลย ทุกตัวเลขมาจากสูตรที่คำนวณเสร็จในเสี้ยววินาที

ผู้ใช้ถามตรง ๆ ว่า "6 stage ได้ใช้โมเดลนั้นจริงหรือไม่" — คำตอบตอนนั้นอธิบายว่า
ไม่ได้ใช้ **แต่ไม่ได้เอาของหลอกออกจากตัวสินค้า** ปล่อยให้ผู้ใช้ที่ไม่ได้ถาม
ยังเห็นชื่อโมเดลปลอมต่อไปอีกหลายเวอร์ชัน การอธิบายไม่ใช่การแก้

### กฎ

1. **ห้ามหน่วงเวลาเพื่อให้ดูเหมือนกำลังทำงาน** ถ้าคำนวณเสร็จใน 0.2 วินาที
   ต้องแสดงผลใน 0.2 วินาที การรอที่ยอมรับได้มีอย่างเดียวคือการรอสิ่งที่เกิดขึ้นจริง
   (เช่น เว้นจังหวะเรียก API ตามโควตา) และต้องเขียนกำกับไว้ในโค้ดว่ารออะไร
2. **ห้ามแสดงชื่อโมเดล เครื่องมือ หรือขั้นตอน ที่ไม่ได้ถูกเรียกใช้จริง**
3. **ห้ามใช้คำที่อ้างความสามารถเกินจริง** เช่น AI / Consensus / Validated
   ในที่ที่ไม่มีสิ่งนั้นอยู่จริง
4. **เมื่อผู้ใช้ถามว่าอะไรคือของจริง ต้องแก้ตัวสินค้า ไม่ใช่แค่ตอบคำถาม**
   ถ้าคำตอบคือ "ไม่ได้ใช้จริง" สิ่งที่ทำให้เข้าใจผิดต้องถูกลบออกในรอบนั้นเลย
5. **ตรวจก่อนบอกว่าแก้แล้ว** v1.0.7 ประกาศว่าลบชื่อโมเดลหมดแล้ว ทั้งที่เหลืออีก
   สามตัว ต้อง grep ทั้งไฟล์และอ่านข้อความจากหน้าจอจริงก่อนรายงานว่าเสร็จ
6. **บอกข้อจำกัดก่อนถูกถาม** ถ้าสิ่งที่ส่งมอบทำน้อยกว่าที่ชื่อหรือหน้าตาบอกไว้
   ต้องพูดออกมาเอง

### วิธีตรวจก่อนปล่อยทุกครั้ง

```bash
# ต้องได้ 0 ทุกบรรทัด
grep -cE "await delay|setTimeout.*[0-9]{3}" <ไฟล์>     # หน่วงเวลาปลอม
grep -cE "qwen|deepseek|llama|claude-|gpt-4|mistral-" <ไฟล์>  # ชื่อโมเดลที่ไม่ได้เรียก
grep -cE "AI Analysis|Consensus (Judge|Validated)" <ไฟล์>     # คำอวดอ้าง
```

## หลักการเบื้องหลังงานนี้

- **L1** โค้ดคำนวณตัวเลขเอง ไม่ให้โมเดลคิดเลข
- **L2** โมเดลอ่านและอธิบาย วัดว่าตรงกันแค่ไหน
- **L3** คนตัดสินเมื่อโมเดลขัดแย้งกัน

ความสอดคล้องระหว่างโมเดล **ไม่ใช่** ความน่าจะเป็นที่คำตอบถูก วัดจริงแล้วได้
φ = +0.916 ระหว่างสองโมเดลที่แม่นที่สุด แปลว่าโมเดลผิดพร้อมกันเกือบตลอด
ห้ามแสดงความสอดคล้องเป็นเปอร์เซ็นต์ความแม่นยำจนกว่าจะ calibrate ด้วยชุดที่รู้เฉลย
(ดู `research/03-experiments/02-error-correlation-finding.md`)
